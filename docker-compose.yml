# version: '3.8'

services:
  app:
    build: .
    ports:
      - '1945:1945'
    depends_on:
      - redis
      - scylla
    environment:
      - CLIENT_ID=wplace
      - CALLBACK_URL=https://wplace.waktaverse.games/auth/callback
      - JWT_SECRET=iloveisgidolsomuch
      - JWT_REFRESH_SECRET=ilovewoowakgoodsomuch
    networks:
      - app-network

  scylla:
    image: scylladb/scylla
    container_name: scylla
    ports:
      - '9042:9042'
    volumes:
      - scylla-data:/var/lib/scylla
    environment:
      - SCYLLA_DC=datacenter1
      - SCYLLA_RACK=rack1
    networks:
      - app-network

  redis:
    image: redis:alpine
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
    networks:
      - app-network

volumes:
  scylla-data:
  redis-data:

networks:
  app-network:
    driver: bridge
